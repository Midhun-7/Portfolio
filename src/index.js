import React from 'react';
import ReactDOM from 'react-dom';
import Home from './Home'; // Assuming your main React component is named App
// import AppRouter from './Router';

ReactDOM.render(
  <React.StrictMode>
    <Home/>
  </React.StrictMode>,
  document.getElementById('root')
);
